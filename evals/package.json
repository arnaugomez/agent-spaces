{
  "name": "@agent-spaces/evals",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "eval": "bun run run.ts",
    "eval:file-operations": "bun run run.ts --filter file-operations",
    "eval:shell-execution": "bun run run.ts --filter shell-execution"
  },
  "dependencies": {
    "@agent-spaces/sdk": "workspace:*",
    "@agent-spaces/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*",
    "bun-types": "*"
  }
}

